# RULES — 규칙의 의미

[CLAUDE.md](CLAUDE.md) 의 규칙이 **왜 있고 어떻게 지키는지**를 적어 둔 곳입니다. CLAUDE.md 는 매 세션 전체가 읽히므로 목록만 짧게 두고, 판단이 애매할 때 여기 해당 절만 펴 봅니다.

이 저장소는 [realitykit-audio-lab](../realitykit-audio-lab) 의 규칙을 물려받되, **설계를 대화로 먼저 정하는 방식**(규칙 1~5)을 더했습니다. 오디오 랩에서는 「무엇을 만들지」가 API 문서에 거의 다 적혀 있었지만, 여기서는 **버퍼를 어떻게 배치할지가 열려 있습니다.** 열린 것을 AI 가 먼저 닫아 버리면 배울 것이 사라집니다.

---

## 규칙 1·2의 의미 — 왜 코드를 바로 주지 않나

오디오 랩의 방식(뼈대 + 「따라 칠 코드」)은 **문법을 손에 익히는 데** 좋았습니다. 하지만 그것만으로는 «무엇을 만들지 정하는 일» 이 통째로 AI 몫으로 남습니다.

그래서 한 단계를 **두 국면**으로 나눕니다.

| 국면 | 누가 | 무엇을 | 결과물 |
|---|---|---|---|
| **A · 설계 대화** | 사용자가 정함 | 데이터 구조 · 타입 이름 · 책임 분배 | `Docs/StepNN.md` 의 「우리가 정한 것」 절 |
| **B · 타이핑** | AI 가 준비, 사용자가 침 | 문법 · 세부 구현 | `.swift` 뼈대 + 「따라 칠 코드」 |

**A 가 끝나기 전에 B 를 시작하지 않습니다.** 뼈대가 먼저 나오면 설계 대화는 «AI 가 정한 것을 맞히는 퀴즈» 가 됩니다.

### 한 답변에 질문 하나

세 개를 한꺼번에 물으면 사용자는 가장 쉬운 것만 답하고 나머지는 «네» 로 흘립니다. 하나씩 물으면 하나씩 생각합니다.

**질문의 모양**

- ❌ 「버퍼 레이아웃을 (가) AoS 로 할까요 (나) SoA 로 할까요?」 — AI 가 만든 기호로 묻고 있습니다
- ✅ 「스플랫 하나의 값 열넷을 한 줄로 죽 이어 붙일까요, 아니면 위치만 모은 줄·크기만 모은 줄로 나눌까요?」

### 「모르겠다」에 답을 주지 않는 순서

ⓐ **어디를 볼지** → ⓑ **무엇과 무엇 중 고르는 문제인지** → ⓒ **뼈대만 비워서** → ⓓ **답**

한 칸씩 내려갑니다. 다만 **「그냥 답 주세요」 라고 하면 즉시 ⓓ 로** 갑니다 — 힌트를 미끼로 쓰지 않습니다.

### 하지 않을 것

- 소크라테스식으로 질문만 던지고 답을 안 주는 것 — **정답 코드는 결국 줍니다** (규칙 6)
- 「스스로 찾아보세요」 로 끝내는 것 — 조사는 AI 몫입니다 (규칙 16)
- 사용자가 지은 이름을 더 좋은 이름으로 계속 고치려 드는 것 — **한 번만 말합니다**

---

## 규칙 3의 의미 — 데이터 구조가 먼저

오디오 랩의 주인공은 «어떤 API 를 언제 부르는가» 였습니다. 이 저장소의 주인공은 다릅니다.

```
RealityKit 은 스플랫 파일을 읽어 주지 않습니다.
그래서 이 저장소의 절반은 «숫자를 어떤 순서로 어디에 놓는가» 입니다.
```

`GaussianSplatComponent(resource)` 는 한 줄이고 5분이면 배웁니다. `stride` 를 바이트로 세는 것, 3DGS PLY 의 필드 순서가 우리 레이아웃과 다르다는 것, 활성화 함수를 언제 적용하는지 — 이쪽이 오래 걸리고, 이쪽이 다른 데서도 쓰입니다.

**그래서 각 단계의 설계 대화는 이 질문부터 시작합니다.**

1. 이 단계가 다루는 데이터는 무엇인가
2. 한 개가 몇 바이트인가
3. 그것이 몇 개인가
4. 누가 그 배치를 아는가 — 파서인가, 조립하는 쪽인가, 그리는 쪽인가

4번이 SOLID 로 이어집니다(규칙 11). 배치를 아는 곳이 셋이 되면 그때 나눕니다.

---

## 규칙 4의 의미 — Productive Failure

Manu Kapur 의 연구가 말하는 것은 단순합니다. **잘 설계된 실패가 잘 설계된 설명보다 오래 남습니다.**

핵심은 «실패해도 괜찮다» 가 아니라 **«생성하고 탐색한 뒤에 설명을 듣는다»** 입니다. 배우는 사람이 먼저 자기 나름의 표현을 만들어 보면 — 대개 틀리게 — 그 시도들이 «무엇이 문제인지» 를 알아볼 수 있는 눈을 만듭니다. 그 상태에서 정답 구조를 들으면 «아 그래서 이렇게 생겼구나» 가 됩니다. 순서를 뒤집으면 정답은 그냥 외울 것이 됩니다.

### 단계 설명서의 세 절

```
① 먼저 해 보기   — 배우기 전에 예측하거나 만들어 보는 자리
② 어긋난 자리    — 예측과 실제의 차이. 표로
③ 정리          — 그제야 정답 구조
```

**①에서 «틀린 답도 괜찮다» 고 말해 두는 것이 중요합니다.** 맞히는 것이 목적이 아니라 **자기 생각을 한번 세워 보는 것**이 목적입니다.

### 이 저장소에서 특히 잘 듣는 자리

스플랫은 **틀렸을 때 화면이 극적으로 이상해집니다.** 이게 큰 자산입니다.

| 단계 | 일부러 겪는 실패 | 나중에 어디서 값을 하나 |
|---|---|---|
| 02 | `offset` 을 4바이트 밀기 | 06 에서 PLY 가 안 뜰 때 «이 화면 봤다» |
| 03 | raw 값을 그대로 넣기 | 06 에서 검은 덩어리가 나올 때 |
| 04 | 불투명도를 낮추고 구름 안으로 들어가기 | 07 의 오버드로 |
| 09 | 조명 슬라이더를 끝까지 올려 보기 | 촬영 단계로 되돌아가는 판단 |

**빗나간 예측은 «틀렸다» 가 아니라 «수확» 으로 적습니다.** PROGRESS 의 세션 로그와 Q&A 에 그렇게 남깁니다.

### 하지 않을 것

- 실패를 숙제처럼 쌓는 것 — 한 단계에 하나면 충분합니다
- 실패시켜 놓고 설명을 안 하는 것 — ③이 없으면 그냥 실패입니다
- **이미 아는 것을 굳이 틀리게 하는 것** — 사용자가 답을 아는 자리에서는 그냥 넘어갑니다

---

## 규칙 5의 의미 — AI 가 가장 잘 안다고 두지 않기

판단이 갈리는 지점에서 AI 가 결론을 먼저 주면, 사용자는 그 결론의 **근거를 확인하지 않고** 받습니다. 두 번째로 비슷한 판단이 왔을 때 여전히 못 합니다.

**형식**

```
선택지 두셋 · 각각의 대가 · AI 의 의견(있으면 «제 생각은» 이라고 밝히고) · 사용자가 정함
```

그리고 **답변마다 사용자가 스스로 생각할 지점을 최소 하나** 남깁니다. 질문이어도 되고, ⭐️ 즉흥 실험이어도 되고, 「이건 나중에 다시 볼 겁니다」 한 줄이어도 됩니다.

---

## 규칙 6·7의 의미 — 뼈대와 「따라 칠 코드」

**뼈대에는 실행되는 줄이 하나도 없습니다.** `import` 한 줄까지 사용자 몫입니다. 코드가 들어갈 자리에는 이 한 줄만 둡니다.

```swift
// 코드 작성: makeSplatBuffer(from:) · SplatLayout · stride
```

**설명을 붙이지 않습니다.** 붙이면 그 설명이 정답 코드의 요약이 되어, 타이핑이 «생각하며 옮기기» 가 아니라 «추측» 이 됩니다.

**정답 코드는 `Docs/<파일명>.md` 의 「따라 칠 코드」에 둡니다.** 대화창을 오가지 않고 Xcode 안에서 나란히 보기 위해서입니다. `.md` 는 컴파일되지 않으니 «주석 해제 작업» 위험도 없습니다. **대화에는 전문을 붙이지 않습니다** — 두 곳에 있으면 하나가 낡습니다.

> **복사–붙여넣기는 하지 않습니다.** 문서가 Xcode 안에 있는 이유는 «창을 안 오가려고» 이지 «복사하려고» 가 아닙니다.

---

## 규칙 8·9·10의 의미 — 지도와 문서화 주석

`.swift` 최상단은 **«지도» 30줄 안팎**입니다. 변수·함수의 이름과 역할, 주고받는 메시지 경로. 그 이상은 `Docs/` 로 갑니다.

설명이 좋아서 코드를 밀어내는 일이 실제로 벌어집니다(오디오 랩 6단계에서 헤더가 190줄이 됐습니다). 지도 끝에 한 줄로 잇습니다.

```
📖 자세한 설명 → Docs/Step01_OneSplat.md
```

선언 위의 `///` 는 그대로 둡니다 — **심볼 하나의 계약**이라 코드와 같은 자리에 있어야 합니다.

| 층 | 무엇 | 어디 |
|---|---|---|
| 지도 (규칙 8) | 파일 하나의 구조 | `.swift` 최상단 |
| `///` (규칙 9) | 심볼 하나의 계약 | 선언 바로 위 |
| 설명서 (규칙 8) | 개념·함정·확인 절차·따라 칠 코드 | `Docs/StepNN.md` |

세 층이 서로 다른 말을 하기 시작하면 그때부터 문서는 도움이 아니라 함정입니다.

---

## 규칙 11의 의미 — SOLID 를 «되도록»

SOLID 는 **규칙이 아니라 진단 목록**으로 씁니다. 다섯 개를 다 채우는 것이 목표가 아니라 **아플 만한 자리를 미리 알아보는 것**이 목표입니다.

### 적용하지 않는 자리

| 신호 | 왜 적용하지 않나 | 대신 |
|---|---|---|
| 한 단계에서만 쓰고 끝날 코드 | 재사용되지 않을 추상은 비용만 남습니다 | 그대로 두고 이유를 한 줄 |
| 아직 한 번도 안 바뀐 자리 | 근거 없는 «전략적 추측» 이 됩니다 | **두 번째로 고칠 때** 엽니다 |
| 프로토콜 하나에 구현이 영원히 하나 | 이름만 늘고 화살표는 그대로 | 구체 타입 그대로 |
| 함께 바뀌는 것을 가르려 할 때 | 인위적인 이음매가 생깁니다 | 한 곳에 |
| 초보자가 «지금 배우는 것» 을 가릴 때 | 이 저장소의 1순위는 API·데이터 학습입니다 | 학습 대상을 앞에 |

**마지막 줄이 가장 자주 걸립니다.** 1단계에서 «버퍼에 숫자를 채우는 것» 을 배우는 사람에게 프로토콜 셋을 먼저 읽히면, 배우려던 것이 뒤로 밀립니다.

### 적용하는 자리

| 신호 | 대개 이것 |
|---|---|
| `switch` 로 종류를 판별하고 있다 | **OCP** |
| 테스트나 실험에서 갈아 끼우고 싶다 | **DIP** |
| 프로토콜 메서드를 빈 구현으로 채웠다 | **ISP** |
| 같은 파일을 서로 다른 이유로 자꾸 고친다 | **SRP** |
| 자식이 부모의 약속을 어긴다 | **LSP** |

**이 저장소에서 DIP 가 열릴 자리는 예고돼 있습니다** — 5·6단계에서 «스플랫 데이터의 출처» 가 둘(코드로 만든 것 / PLY 에서 읽은 것)이 되는 지점입니다. 그전까지는 열지 않습니다.

### 표시

```swift
// [DIP] 스플랫의 «출처» 를 프로토콜로 받아, 그리는 쪽이 파일 형식을 모르게 함
private let source: SplatSource
```

두 줄로 넘어가면 그건 `///` 몫입니다. 눈에 띄게 적용할 만한데 일부러 안 했다면 **그것도 한 줄 남깁니다** — 안 하는 판단이 하는 판단보다 배우기 어렵기 때문입니다.

---

## 규칙 12의 의미 — 「다 넣었어」

규칙 6 때문에 **코드를 치는 사람과 문서를 고치는 쪽이 갈라져 있습니다.** 둘이 만나는 지점을 한 번은 대조해야 합니다.

**기억이나 대화 내용으로 판단하지 않고 파일을 직접 엽니다.** 볼 것은 넷 — ① 그대로 들어갔나 ② 제시하지 않은 것이 딸려 왔나 ③ 거짓이 된 주석이 있나 ④ 아무도 안 부르는 것이 남았나.

「그대로 / 어긋남 / 누락」으로 나눠 보고하고, **없으면 "없음" 이라고 분명히 말합니다.** 없는 문제를 만들어 내지 않습니다.

---

## 규칙 13·14의 의미 — 눈으로 확인하기

오디오 랩에는 «귀로 확인하는 법» 이 있었습니다. 여기서는 눈인데, **«보인다» 만으로는 부족합니다.**

스플랫은 그럴듯하게 틀립니다. 개수를 절반만 읽어도 그럴듯하고, 색이 반쯤 어긋나도 그럴듯합니다. 그래서 확인 절차는 **숫자를 포함해야** 합니다.

```
화면에 늘 띄워 둘 것 — 스플랫 개수 · FPS · 버퍼 바이트 수 · 마지막 에러
```

오디오 랩에서 「충돌 N 회」 를 띄워 둔 것이 세 번 다 결정적이었습니다. 여기서도 같습니다.

### ⭐️ 즉흥 실험

규정된 확인 절차 옆에 **한 줄로 바꿔 볼 수 있는 것** 하나. **결과를 알려 주지 않습니다.** 「이렇게 하면 이렇게 됩니다」 가 아니라 「이렇게 해 보세요」.

한 단계에 하나면 충분합니다.

---

## 규칙 15의 의미 — iOS 기준, 시뮬레이터 금지

사용자에게 visionOS 기기가 없습니다. 그리고 `GaussianSplatComponent` 는 **iOS 27 에도 있습니다** — visionOS 로 갈 이유가 없습니다.

- `RealityViewContent` 는 **visionOS 전용**입니다. iOS 에서는 `RealityViewCameraContent`
- **시뮬레이터로 성능을 판단하지 않습니다.** 맥북 에어에서 느린 것은 스플랫이 무거워서가 아니라 시뮬레이터라서입니다
- 배치·색·동작은 시뮬레이터로 봐도 되지만, **「부드러운가」 는 반드시 아이폰에서**

---

## 규칙 16의 의미 — 기억이 아니라 문서

**이 API 는 2026년에 나왔습니다.** 훈련 데이터가 얇고, 그럴듯하게 틀린 코드를 지어내기 가장 쉬운 조건입니다.

- 심볼 이름·시그니처·가용성은 **매번 공식 문서에서 확인**합니다
- 확인한 문서는 링크로 남깁니다 (규칙 16)
- 자주 여는 것은 [APPLE_DOCS.md](APPLE_DOCS.md) 에 모읍니다
- 문서에 없는 것은 **«없다» 가 아니라 «못 찾았다»** 로 적고 PROBLEMS 로 보냅니다

> Apple 문서 페이지는 자바스크립트로 그려집니다. URL 뒤에 **`.md` 를 붙이면** 원문 마크다운이 그대로 나옵니다 — 가용성 주석과 코드 예제까지 전부. AI 가 문서를 확인할 때 이 방법을 씁니다.

---

## 규칙 17~19의 의미 — 기록을 참으로 유지하기

| 성격 | 파일 |
|---|---|
| 지금 어디까지 왔나 | [PROGRESS.md](PROGRESS.md) |
| 단계별 계획, 완료 체크 | [README.md](README.md) 의 «진행 상황» 표 |
| 용어의 뜻 | [STUDY.md](STUDY.md) |
| 막혔던 것과 그 **해결** | [Q&A.md](Q&A.md) |
| **아직 못 푼 것** — 재현되지만 우회 중 | [PROBLEMS.md](PROBLEMS.md) |

**같은 사실을 두 곳에 쓰지 않습니다.** 중복은 낡은 쪽이 거짓말을 하게 만듭니다.

### 비유 (규칙 19)

STUDY 의 용어 표에는 «뜻» 오른쪽에 **«비유»** 열을 둡니다. 코드 밖의 익숙한 것에 빗대되, **그 비유가 어디서 깨지는지도 함께** 적습니다. 깨지는 지점을 안 적으면 비유가 오해로 굳습니다.

비유가 잡음이 될 자리에는 `—` 를 둡니다. 억지로 채우지 않습니다.

---

## 규칙 20의 의미 — 핵심을 맨 앞에

요약을 결론이 아니라 **맨 앞**에 둡니다. 10문장 이내로, 강조해서. 자세한 설명은 그 뒤에 잇습니다.

읽는 사람이 중간에 그만둬도 가장 중요한 것은 이미 읽은 상태여야 합니다.

---

## 규칙 21·22의 의미 — 세션을 잇기

새 세션은 **PROGRESS.md 만 읽고 시작합니다.** 저장소를 훑거나 README 전체를 읽지 않습니다. 거기 없는 것이 필요할 때만 «파일 지도» 를 보고 그 파일의 **필요한 부분만** 엽니다.

**PROGRESS.md 의 상한은 100줄입니다.** 넘으면 오래된 것을 각자의 집으로 보내고 여기서 지웁니다.

---

## 규칙 23의 의미 — 개념을 «문제» 로 바꾸기

용어집(규칙 18)은 **읽는 것**이고, 질문 기록은 **푸는 것**입니다. 같은 개념이라도 읽은 것과 답을 세워 본 것은 다르게 남습니다.

### 무엇을 문제로 만드나

기준은 하나 — **«알아 두면 이 저장소 밖에서도 쓰이는가».**

| 문제로 만든다 | 그냥 알려 준다 |
|---|---|
| 바이트 계산 · 메모리 정렬 · 패딩 | 이 API 의 심볼 이름 |
| 부동소수점 · 엔디안 · 정밀도 | Xcode 설정 경로 |
| 좌표계 · 쿼터니언 · 행렬 | 파일을 어디에 두는지 |
| 시간복잡도 · 대역폭 · 캐시 | 오늘 무엇을 할지 |
| 왜 이 자료구조인가 | 오타·빌드 오류 |

오른쪽 것을 문제로 내면 **진도를 막는 관문**이 됩니다. 왼쪽 것만 냅니다.

### 문제의 모양

- **한 답변에 하나** (규칙 2 가 그대로 적용됩니다)
- **답이 숫자거나 한 문장**이어야 합니다. 「설명해 보세요」 는 문제가 아니라 숙제입니다
- **틀려도 괜찮다고 말해 둡니다.** 맞히는 것이 목적이 아니라 자기 생각을 한번 세워 보는 것이 목적입니다

### 맞혔을 때가 더 중요합니다

정답이 나오면 **거기서 끝내지 않고 «한 겹 더 깊은 것» 을 다음 문제로** 냅니다.

> Q1 이 그랬습니다. 「스플랫 하나는 몇 바이트인가」의 답 56 은 그 자체로는 산수입니다. **진짜 배울 것은 «그런데 Apple 샘플의 stride 는 60이다» 에 있었습니다.** 첫 문제는 두 번째 문제를 세우기 위한 발판입니다.

틀렸으면 **그 어긋남 자체가 다음 문제**가 됩니다. 어느 쪽이든 이어집니다.

### 기록 형식

`STUDY.md` 의 「질문 기록」에 네 가지를 남깁니다.

```
① 질문   ② 사용자의 답   ③ 정답   ④ 어긋난 자리에서 배운 것
```

**④가 가장 중요합니다.** ③만 있으면 정답표고, ④가 있어야 배운 기록입니다. 관련 용어에는 앵커 링크로 이어 둡니다 — 몇 주 뒤에 **질문만 보고 다시 풀** 때 답이 바로 눈에 안 들어와야 합니다.

---

## 규칙 1 의 ⓑ — «막힐 요소를 먼저 주고 시간을 준다»

2026-09-03 에 사용자가 직접 요청한 방식입니다.

> 「앞으로 뭔가 할 때 막힐 요소를 먼저 주고, 내가 생각하고 리서치할 시간을 주고, 힌트를 조금씩 달라하고 내가 정말 모른다고 말할 때는 정답을 알려줘.」

### 왜 이게 필요한가

「따라 칠 코드」를 **미리** 문서에 넣어 두면, 아무리 «먼저 해 보세요» 라고 적어 두어도 사람은 옆의 정답을 봅니다. 그러면 남는 것은 «타이핑 연습» 이고, **찾아본 기억**이 안 남습니다.

Productive Failure(규칙 4)의 전제가 «생성하고 탐색한 뒤에 설명을 듣는다» 인데, 정답이 옆에 있으면 생성 단계가 통째로 사라집니다. **규칙 4 를 실제로 작동시키는 장치가 이 ⓑ 입니다.**

### 막힐 요소를 «어디까지» 알려 주나

| 준다 | 안 준다 |
|---|---|
| **무엇이 어려운지** — 「iOS 의 RealityView 는 타입이 다릅니다」 | 그 타입의 이름 |
| **어느 문서를 볼지** — 링크 | 그 문서에서 뽑아낸 답 |
| **어떤 종류의 실수가 나오는지** — 「visionOS 예제를 그대로 베끼면 컴파일이 안 됩니다」 | 고친 코드 |
| **다 됐는지 판정하는 법** — 눈으로 확인 절차 | — |

경계는 이것입니다 — **«찾을 곳» 은 주고 «찾은 것» 은 안 줍니다.**

### 힌트의 네 칸

사용자가 요청할 때마다 **한 칸씩** 내려갑니다. 미리 여러 칸을 주지 않습니다.

```
① 어디를 볼지        문서 링크, 또는 「이건 카메라 설정 문제입니다」
② 무엇과 무엇 중      「두 가지 중 하나를 고르는 문제입니다 — 실제 카메라를 쓸까, 안 쓸까」
③ 뼈대만 비워서      content.camera = ____
④ 답                코드
```

**④ 로 가는 조건은 하나입니다** — 사용자가 「모르겠다」 또는 「답 주세요」 라고 **말했을 때.** 시간이 오래 걸린다고 AI 가 알아서 내려가지 않습니다. 다만 **「그냥 답 주세요」 라고 하면 ①②③ 을 건너뛰고 즉시 ④** 로 갑니다 — 힌트를 미끼로 쓰지 않습니다.

### 하지 않을 것

- **막힐 요소를 예고하면서 답까지 흘리는 것.** 「`RealityViewContent` 가 아니라 `RealityViewCameraContent` 입니다」 는 예고가 아니라 답입니다. 「iOS 는 타입이 다릅니다 — 문서에서 확인해 보세요」 가 예고입니다
- **막힐 요소를 너무 많이 세는 것.** 한 단계에 **셋 안팎**. 다섯 개가 넘으면 그건 단계를 쪼개야 한다는 신호입니다
- **혼자 해 볼 시간을 «과제» 로 만드는 것.** 시도하지 않고 바로 답을 달라고 해도 그냥 줍니다. 압박하지 않습니다

### ⚠️ 실제로 한 번 실패했습니다 — «증상» 만 적고 «할 일» 을 안 적은 것

0단계의 첫 판이 이랬습니다.

> 「그냥 두면 AR 모드로 뜹니다. → 둘 중 하나를 고르는 문제입니다. 카메라 다루는 속성을 찾아보세요.」

사용자의 반응은 이랬습니다 — **「둘 중 하나가 뭘 고르라는 거지? 속성을 찾아보라는 게 어디서 뭘 찾으라는 거야?」**

맞는 지적입니다. 저 문장은 **증상**(AR 모드로 뜬다)과 **암시**(둘 중 하나)만 있고, **할 일**이 없습니다. 「먼저 해 보세요」 가 성립하려면 **무엇을 해 보라는 것인지는 분명해야** 합니다.

**고친 판**

> **할 일** — 클로저 안에서 `content` 의 `camera` 속성에 값을 하나 대입하는 것.
> **고르는 것** — 그 타입에 값이 둘 있습니다. 문서 첫 줄이 그대로 알려 줍니다 — *"can be world tracking or virtual"*. 우리는 AR 을 안 씁니다. 어느 쪽일까요?

**할 일과 선택지는 다 줬는데도 정답은 아직 안 준** 상태입니다. 이게 ⓑ 의 모양입니다.

### 예고에 반드시 들어갈 세 줄

| | 예 |
|---|---|
| **할 일** — 무엇을 만들거나 정해야 하나 | 「`camera` 속성에 값 하나를 대입한다」 |
| **왜 문제인가** — 안 하면 무슨 일이 나나 | 「안 쓰면 AR 모드로 떠서 카메라 권한을 묻는다」 |
| **고르는 것** — 갈림길이 있으면 갈래를 다 보인다 | 「world tracking 이냐 virtual 이냐」 |

**«찾아보세요» 로 끝나는 문장은 예고가 아닙니다.** 무엇을 찾는지까지 적고, 찾은 결과만 비웁니다.
