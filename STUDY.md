# STUDY — 용어집과 질문 기록

두 가지를 여기 쌓습니다.

- **[용어집](#용어집)** — ⭐️GUIDE⭐️ 로 짚은 용어 (규칙 18). «비유» 열에는 코드 밖의 익숙한 것에 빗댄 설명과, **그 비유가 어디서 깨지는지**를 함께 적습니다 (규칙 19). 비유가 잡음이 될 자리에는 `—` 를 둡니다.
- **[질문 기록](#질문-기록)** — 대화 중에 한 문제씩 던진 컴퓨터 과학·학습 주제 질문 (규칙 23). 질문 · 내 답 · 정답 · **어긋난 자리에서 배운 것**을 함께 남깁니다.

> **왜 답을 안 적고 질문으로 남기나** — 답을 읽은 것과 답을 세워 본 것은 다르게 남습니다. 여기 쌓인 질문들은 나중에 **다시 풀어 볼 문제집**이 됩니다. 정답만 보고 넘어가지 말고, 몇 주 뒤에 질문만 보고 다시 답해 보세요.

---

# 질문 기록

## Q1 · 스플랫 하나는 몇 바이트인가

**언제** — 2026-09-03 · 1단계 설계 대화
**주제** — 메모리 정렬 (alignment)

**질문** — 스플랫 하나가 갖는 값이 position `float 3` · scale `float 3` · rotation `float 4` · opacity `float 1` · SH 0차 `float 3` 일 때, 스플랫 하나는 몇 바이트일까?

**내 답** — 55 바이트

**정답** — **56 바이트.** `3 + 3 + 4 + 1 + 3 = 14` 개의 `Float`, `Float` 하나가 4바이트이므로 `14 × 4 = 56`.

**어긋난 자리에서 배운 것** — 하나 차이는 세다가 놓친 것이라 크게 중요하지 않습니다. **진짜 배울 것은 그다음에 있었습니다** — Apple 공식 샘플의 `stride` 는 **56이 아니라 60바이트**(15 float)입니다.

```swift
let stride = 15 * floatSize   // 60바이트. 우리가 센 것은 56인데?
```

남는 4바이트는 **낭비가 아니라 정렬(alignment)** 입니다. GPU 는 메모리를 16바이트 단위로 읽는 것이 가장 빠른데, 56은 16의 배수가 아닙니다(16×3.5). 60도 16의 배수는 아니지만, `LowLevelBuffer` 를 만들 때 `sizeMultiple: 16` 으로 전체 크기를 16의 배수로 맞추고 있습니다.

**여기서 얻은 규칙 하나** — **`stride` 는 «값의 합» 이 아니라 «다음 것이 시작하는 자리»** 입니다. 둘이 같을 거라고 가정하면 2단계에서 화면이 무너집니다.

**연결** — [메모리 정렬](#데이터-배치) · [stride](#데이터-배치) → 이어지는 문제 [Q2](#q2--5615--0xf-는-얼마인가)

---

## Q2 · `(56 + 15) & ~0xF` 는 얼마인가

**언제** — 2026-09-03 · 1단계 설계 대화 ([Q1](#q1--스플랫-하나는-몇-바이트인가) 에서 이어짐)
**주제** — 비트 마스크 · 정렬 올림 관용구

**질문** — Apple 공식 샘플의 버퍼 크기 계산식이 `((data.length + 15) & ~0xF)` 다. 스플랫 하나가 56바이트일 때 `(56 + 15) & ~0xF` 는 몇일까?
(`0xF` = 15 = 이진 `1111`, `~` 는 비트 반전, `&` 는 둘 다 1인 자리만 1)

**내 답** — 15. *「`&` 라서 `1111` 부분만 살아남지 않을까」*

**정답** — **64.**

```
0xF       = 0000 1111     ← 이걸로 & 하면 하위 4비트만 살아남음
~0xF      = 1111 0000     ← 실제로 쓰는 것. 살아남는 쪽이 정반대
71        = 0100 0111
71 & ~0xF = 0100 0000  =  64
```

**어긋난 자리에서 배운 것** — **`&` 가 «둘 다 1인 자리만 살린다» 는 이해는 정확했습니다.** 놓친 것은 `~` 하나예요. 그 물결표가 마스크를 뒤집어서 **«살아남는 쪽» 이 정반대**가 됩니다.

| 식 | 하는 일 | 이름 |
|---|---|---|
| `n & 0xF` | 하위 4비트만 남김 | **16으로 나눈 나머지** |
| `n & ~0xF` | 하위 4비트를 지움 | **16의 배수로 내림** |
| `(n + 15) & ~0xF` | 내리기 직전에 밀어 올림 | **16의 배수로 올림** ← 이것 |

내림 함수 앞에 «한 칸 모자란 값(15)» 을 더해서 올림을 만드는 것이 요령입니다. `56 → 64`, `64 → 64`(이미 배수면 그대로), `65 → 80`.

**어디서 또 쓰이나** — 정렬이 필요한 곳이면 어디든 나옵니다. 메모리 할당, 파일 블록 크기, 디스크 섹터, 네트워크 패킷 패딩. **`2^k` 의 배수로 올림하려면 `(n + (2^k - 1)) & ~(2^k - 1)`** 입니다. 나눗셈보다 훨씬 빨라서 관용구로 굳었습니다.

**연결** — [비트 마스크](#데이터-배치) · [메모리 정렬](#데이터-배치)

---

## Q3 · 왜 이름에 `SystemDefault` 가 들어갈까

**언제** — 2026-09-03 · 0단계
**주제** — 여러 플랫폼이 공유하는 API 의 흔적

**질문** — 아이폰은 GPU 가 하나뿐인데, 왜 Apple 은 함수 이름을 `MTLCreateDevice()` 가 아니라 **`MTLCreateSystemDefaultDevice()`** 로 지었을까?

**내 답** — 「시스템이 기본으로 고른 그래픽 카드」

**정답** — 뜻은 정확합니다. **«왜 그 말이 필요했나» 가 나머지 반입니다** — **맥은 GPU 가 여러 개일 수 있기 때문입니다.** 외장 GPU 를 단 맥, 내장·외장을 함께 가진 맥에서는 「그냥 GPU 줘」 가 성립하지 않습니다. 「**그중 기본으로 정해진 것** 줘」 라고 해야 말이 되죠.

**어긋난 자리에서 배운 것** — **Metal 은 iOS·macOS·tvOS·visionOS 가 함께 쓰는 하나의 API 입니다.** 그래서 **맥에서만 의미가 있는 사정이 아이폰 쪽에도 그대로 남습니다.**

같은 이유가 **옵셔널**도 설명합니다.

| 남은 흔적 | 왜 | 아이폰에서는 |
|---|---|---|
| 이름의 `SystemDefault` | 맥은 GPU 가 여럿일 수 있음 | 늘 그 하나 |
| 반환값이 옵셔널 `?` | 화면 없는 서버·일부 가상 환경은 GPU 를 못 잡음 | 사실상 항상 성공 |

**여기서 얻은 규칙 하나** — **API 의 이름이나 옵셔널이 «지금 내 플랫폼에서는» 과해 보이면, 그 API 를 함께 쓰는 다른 플랫폼을 의심해 보세요.** 대개 거기서는 그 말이 꼭 필요합니다. 앞으로 Apple API 를 읽을 때 계속 쓰이는 눈입니다.

**연결** — [`MTLDevice`](#데이터-배치) · [핸들](#데이터-배치)
---

## Q4 · `MemoryLayout<SIMD3<Float>>.size` 는 몇인가

**언제** — 2026-09-03 · 1단계 설계 대화
**주제** — 벡터 타입의 패딩 ([Q2](#q2--5615--0xf-는-얼마인가) 의 정렬 이야기가 여기서 다시 나옵니다)

**질문** — `Float` 하나가 4바이트, 셋이면 12. 그런데 12는 16의 배수가 아니다. `SIMD3<Float>` 는 몇 바이트일까?

**내 답** — 16 바이트

**정답** — **16 바이트. 맞혔습니다.**

**배운 것** — `SIMD3<Float>` 는 «Float 세 개» 가 아니라 **«네 칸짜리 자리에 세 개만 채운 것»** 입니다. 남는 4바이트는 [패딩](#데이터-배치) 이고, 이유는 Q2 와 같습니다 — **GPU 는 16바이트 단위로 읽는 것이 가장 빠릅니다.**

**따름결과 — `SIMD4<Float>` 도 똑같이 16바이트입니다.** 셋을 쓰나 넷을 쓰나 메모리는 한 톨도 안 아껴집니다. 그래서 그래픽 코드에서 «어차피 자리가 남으니 하나 더 담자» 는 관행이 생깁니다 (색에 알파를 끼워 넣는 것처럼).

**여기서 얻은 규칙 하나** — **«필드의 크기를 합치면 구조체의 크기» 라는 가정은 틀립니다.** 이 가정이 다음 문제([Q5](#q5--스플랫-구조체-전체는-몇-바이트인가))에서 한 번 더, 더 크게 어긋납니다.

**연결** — [패딩](#데이터-배치) · [메모리 정렬](#데이터-배치) · [Q1](#q1--스플랫-하나는-몇-바이트인가) · [Q2](#q2--5615--0xf-는-얼마인가)
---

## Q5 · 스플랫 구조체 전체는 몇 바이트인가

**언제** — 2026-09-03 · 1단계 설계 대화 ([Q4](#q4--memorylayoutsimd3floatsize-는-몇인가) 에서 이어짐)
**주제** — 구조체 안의 패딩. 필드 «사이» 에도 빈틈이 생긴다

**질문** — 이 구조체의 `MemoryLayout<Splat>.size` 는?

```swift
struct Splat {
    var position: SIMD3<Float>    // 16
    var scale:    SIMD3<Float>    // 16
    var rotation: simd_quatf      // 16
    var opacity:  Float           //  4
    var sh:       SIMD3<Float>    // 16
}                                 // 합치면 68
```

**내 답** — 80 바이트

**정답** — **80 바이트. 맞혔습니다.**

```
바이트  0        16       32       48   52          64       80
       ├────────┼────────┼────────┼────┼───────────┼────────┤
       │position│ scale  │rotation│opa │  버려짐    │   sh   │
       └────────┴────────┴────────┴────┴───────────┴────────┘
                                        ↑ 12바이트 패딩
```

`opacity` 가 52에서 끝나는데 `sh` 는 **16의 배수 주소에서만** 시작할 수 있습니다. 52 다음 16의 배수는 64 — [Q2](#q2--5615--0xf-는-얼마인가) 의 `(52 + 15) & ~0xF` 가 여기서 세 번째로 쓰였습니다.

**배운 것** — Q4 의 패딩은 **타입 «안»** 에 있었지만, 여기서는 **필드와 필드 «사이»** 에 생깁니다. 같은 정렬 규칙인데 나타나는 자리가 다릅니다.

**얼마나 손해인가**

| | 값 |
|---|---|
| Apple 이 요구하는 스플랫 하나 | **56바이트** |
| 우리 구조체 | **80바이트** |
| 차이 | **24바이트, 43% 더 큼** |
| 스플랫 50만 개면 | **12MB 를 그냥 버림** |

**여기서 얻은 규칙 하나** — **Swift 가 정한 메모리 배치는 내가 통제하지 못합니다.** 편하게 읽으라고 만든 구조체가 GPU 가 원하는 모양과 다른 것은 «버그» 가 아니라 **원래 다른 목적의 물건**이기 때문입니다.

**한 겹 더 — 이것이 2단계입니다.** 그럼 80바이트짜리 구조체를 어떻게 56바이트짜리 버퍼로 옮길까? 답은 2단계에서 직접 부딪혀 찾습니다.

**연결** — [메모리 정렬](#데이터-배치) · [패딩](#데이터-배치) · [stride](#데이터-배치) · [Q1](#q1--스플랫-하나는-몇-바이트인가) · [Q4](#q4--memorylayoutsimd3floatsize-는-몇인가)
---

# 용어집

## 가우시안 스플랫 기본

| 용어 | 뜻 | 비유 |
|---|---|---|
| **3D Gaussian Splatting (3DGS)** | 장면을 삼각형이 아니라 «뿌연 타원체» 수십만 개로 표현하는 렌더링 기법 | **에어브러시로 찍은 점 수십만 개.** 가까이 가면 각각이 흐릿한 얼룩인데, 멀리서 보면 사진이 됩니다. — **깨지는 곳** : 에어브러시 점은 평면에 있지만 스플랫은 공간에 떠 있고 방향에 따라 다르게 보입니다 |
| **스플랫(splat)** | 타원체 하나. 위치·크기·회전·불투명도·색을 가짐 | — |
| **구면조화 (Spherical Harmonics, SH)** | 보는 각도에 따라 색이 달라지게 하는 계수 묶음. **0차면 어디서 봐도 같은 색** | **홀로그램 스티커.** 각도를 바꾸면 색이 변합니다. 차수가 높을수록 변화가 정교합니다 — **깨지는 곳** : 스티커는 무늬가 바뀌지만 SH 는 «색과 밝기» 만 바뀝니다 |
| **SfM (Structure from Motion)** | 여러 장의 사진에서 «각 사진이 어디서 찍혔는지» 와 성긴 점군을 복원하는 단계 | **여러 각도의 관광 사진으로 건물의 입체 배치를 되짚는 일.** 같은 창문이 여러 사진에 나오면 카메라 위치를 역산할 수 있습니다 |
| **학습(training) / 최적화** | 성긴 점을 타원체로 부풀려 원본 사진과 맞아떨어질 때까지 값들을 조정하는 단계 | — |
| **pruning** | 학습 중에 기여가 적은 스플랫을 솎아 내는 것. **개수를 근본적으로 줄이는 유일한 방법** | — |
| **오버드로 (overdraw)** | 한 픽셀 위에 여러 스플랫이 겹쳐 그려지는 것. **개수가 같아도 화면을 덮는 면적이 크면 느려집니다** | **유리창에 겹쳐 붙인 반투명 시트.** 장수가 같아도 넓게 붙이면 빛이 더 많이 막힙니다 |
| **4DGS** | 시간축이 있는 스플랫. 프레임마다 다른 스플랫 묶음을 재생 | **플립북.** 각 장이 3D 스플랫입니다 |

---

## 데이터 배치

| 용어 | 뜻 | 비유 |
|---|---|---|
| **버퍼 (buffer)** | GPU 가 읽는 «숫자만 죽 늘어놓은 메모리» | **주소 없이 이어 붙인 긴 종이 띠.** 어디부터 무엇인지는 따로 약속해야 압니다 |
| **`LowLevelBuffer`** | RealityKit 이 주는 그 버퍼. iOS 26 부터 | — |
| **`Float`** | 32비트(**4바이트**) 부동소수점. Swift 의 `Double` 은 8바이트 | — |
| **stride** | 한 스플랫에서 **다음 스플랫까지의 바이트 수.** ⚠️ «값의 합» 과 같지 않을 수 있습니다 ([Q1](#q1--스플랫-하나는-몇-바이트인가)) | **좌석 간격.** 사람 폭이 아니라 «다음 사람까지의 거리» 입니다. 팔걸이(패딩) 때문에 사람 폭보다 큽니다 |
| **offset** | 버퍼 시작점에서 그 속성까지의 **바이트 수** | **좌석 안에서 «컵홀더는 왼쪽 몇 cm»** 같은 것 |
| **메모리 정렬 (alignment)** | GPU·CPU 가 읽기 좋도록 시작 주소를 16바이트 같은 배수에 맞추는 것. **그래서 남는 바이트(패딩)가 생깁니다** | **책장 칸 높이.** 책이 낮아도 칸 높이는 정해져 있습니다 — **깨지는 곳** : 책장은 남는 공간에 다른 걸 넣을 수 있지만 패딩은 못 씁니다 |
| **패딩 (padding)** | 정렬을 맞추려고 끼워 넣는, 아무 의미 없는 바이트 | — |
| **비트 마스크 (bit mask)** | 다른 수와 `&` 해서 **원하는 자리만 남기거나 지우는** 수. `~` 를 붙이면 살리는 쪽이 정반대가 됩니다 ([Q2](#q2--5615--0xf-는-얼마인가)) | **구멍 뚫린 판을 종이 위에 덮는 것.** 구멍 난 자리만 보입니다 — **깨지는 곳** : `~` 는 판과 구멍을 통째로 맞바꿉니다 |
| **정렬 올림 관용구** | `(n + 15) & ~0xF` — 16의 배수로 올림. 일반형은 `(n + (2^k - 1)) & ~(2^k - 1)` | — |
| **인터리브 / 구조체 배열 (AoS)** | 스플랫 하나의 값을 죽 이어 붙이고, 그것을 반복 | **명함첩.** 한 장에 이름·전화·주소가 다 있고, 그런 장이 여러 개 |
| **속성별 버퍼 / 배열의 구조체 (SoA)** | 위치만 모은 줄, 크기만 모은 줄… 로 나누어 저장 | **엑셀 열.** 이름 열, 전화 열이 따로. **하나의 열만 고칠 때 유리합니다** (11단계) |
| **half precision** | 부동소수점을 16비트(2바이트)로. 메모리 절반, 정밀도 절반 | — |
| **활성화 함수 (activation function)** | 저장된 날값을 실제 값으로 바꾸는 함수. 불투명도는 **시그모이드**, 크기는 **exp** | **압축 파일.** 저장할 때 줄여 두고 쓸 때 푸는데, 푸는 방식을 모르면 쓰레기로 보입니다 |
| **로짓 (logit)** | 시그모이드를 통과하기 «전» 의 값. 범위가 −∞~∞ | — |
| **엔디안 (endianness)** | 여러 바이트짜리 숫자를 메모리에 놓는 순서. 작은 자리부터면 little-endian | **날짜를 «03/09/2026» 로 쓰나 «2026/09/03» 로 쓰나** 의 차이. 값은 같은데 읽는 약속이 다릅니다 |
| **PLY** | 3D 점 목록을 담는 파일 형식(1994, 스탠퍼드). **헤더는 사람이 읽는 텍스트, 본문은 이진.** 3DGS 표준 PLY 는 점 하나에 **필드 62개** — 그중 우리가 쓰는 건 **14개** | **엑셀 CSV.** 첫 줄에 열 이름이 있고 아래로 데이터가 쭉 — **깨지는 곳** : CSV 는 본문도 글자라 열어 볼 수 있지만 PLY 본문은 이진이라 깨져 보입니다 |
| **핸들 (handle)** | 직접 만질 수 없는 자원(GPU·파일·창)을 **대신 가리키는 객체.** 모든 요청이 이것을 통과합니다 | **물품보관함 열쇠.** 열쇠가 물건은 아니지만 열쇠 없이는 못 꺼냅니다 — **깨지는 곳** : 열쇠는 잃어버리면 끝이지만 핸들은 대개 다시 얻을 수 있습니다 |
| **`MTLDevice`** | **GPU 칩 하나를 대표하는 핸들.** 버퍼 만들기·셰이더 컴파일·「이 기능 되니?」 질문이 전부 이 객체를 거칩니다 | **관공서 창구.** 건물 안으로 직접 들어갈 수 없고 창구에 대고 요청합니다 — **깨지는 곳** : 창구는 여럿이지만 이 객체는 보통 앱당 하나면 충분합니다 |
| **`MTLCreateSystemDefaultDevice()`** | 「이 기기에서 기본으로 쓸 GPU 를 하나 달라」는 **전역 함수.** 옵셔널을 돌려줍니다 | — |
| **Metal** | Apple 의 GPU 저수준 API. **RealityKit 은 그 위에 얹혀 있습니다** — 스플랫도 결국 Metal 을 통해 그려집니다 | **RealityKit 이 자동변속기라면 Metal 은 수동변속기.** 같은 엔진을 쓰지만 다루는 층이 다릅니다 |

---

## RealityKit

| 용어 | 뜻 | 비유 |
|---|---|---|
| **엔티티 (Entity)** | 씬에 놓이는 «물건» 하나. 그 자체로는 아무 성질이 없습니다 | **빈 옷걸이.** 무엇을 거느냐로 정체가 정해집니다 |
| **컴포넌트 (Component)** | 엔티티에 붙이는 성질. 모양·물리·소리·스플랫… | **옷걸이에 거는 옷.** 여러 벌을 겹쳐 걸 수 있습니다 |
| **ECS (Entity-Component-System)** | 상속 대신 «붙이기» 로 기능을 조합하는 구조 | — |
| **`RealityView`** | SwiftUI 안에 3D 씬을 놓는 뷰 | — |
| **`RealityViewContent`** | ⚠️ **visionOS 전용.** iOS 는 `RealityViewCameraContent` | — |
| **`GroundingShadowComponent`** | 바닥에 근사 그림자를 만드는 컴포넌트. 스플랫에는 **구형 프록시**로 대충 만듭니다 | **물건 밑에 깔아 두는 회색 종이.** 모양이 정확하진 않지만 «떠 있지 않다» 는 느낌은 줍니다 |
| **투명 패스 (transparency pass)** | 반투명한 것들을 뒤에서 앞으로 순서대로 겹쳐 그리는 단계 | — |
| **미터 단위** | RealityKit 의 길이는 **전부 미터**입니다. `size: 1` 은 한 변 1m 인 상자 | **설계도의 축척.** 숫자를 읽기 전에 단위를 봐야 합니다 — **깨지는 곳** : 설계도는 축척을 바꿀 수 있지만 RealityKit 은 미터로 고정입니다 |
| **타입 메서드 (static)** | 인스턴스가 아니라 **타입에 속한** 함수. `MeshResource.generateBox(...)` 처럼 소속을 밝혀 부릅니다 | — |

---

## 이 저장소의 작업 방식

| 용어 | 뜻 | 비유 |
|---|---|---|
| **설계 대화 (국면 A)** | 코드를 치기 전에 데이터 구조·이름·책임을 대화로 정하는 자리. **AI 는 여기서 코드를 주지 않습니다** | — |
| **뼈대 (skeleton)** | 실행되는 줄이 하나도 없는 `.swift`. 주석과 `// 코드 작성:` 만 | **악보의 마디줄.** 어디에 무엇이 올지는 그려져 있고 음표는 비어 있습니다 |
| **「따라 칠 코드」** | `Docs/StepNN.md` 안의 정답 코드. Xcode 에서 나란히 띄우고 **손으로 칩니다** | — |
| **Productive Failure** | 배우기 전에 먼저 시도해 보게 하고, 그 시도가 어긋난 자리를 발판으로 정답을 설명하는 방식 (Manu Kapur) | **길을 헤매 본 동네가 오래 기억에 남는 것.** 지도부터 외운 동네는 금방 잊습니다 |
| **⭐️ 즉흥 실험** | 확인 절차 옆에 붙는, 한 줄로 바꿔 볼 수 있는 것. **결과는 안 알려 줍니다** | — |
| **수확** | 예측이 빗나갔을 때 남는 것. «틀렸다» 가 아니라 이렇게 적습니다 | — |
