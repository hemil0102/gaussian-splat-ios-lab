# PROGRESS

**새 세션은 이 파일만 읽고 시작합니다.** 저장소를 훑거나 README 전체를 읽지 않습니다.
여기에 없는 것이 필요할 때만 아래 [파일 지도](#파일-지도)를 보고 그 파일의 **필요한 부분만** 엽니다.

- 갱신 시점 : 2026-09-03
- 이 파일의 상한 : **100줄**. 넘으면 오래된 것을 각자의 집(README·STUDY·Q&A·PROBLEMS)으로 보내고 여기서 지웁니다.

---

## 지금

이 몇 줄만 읽어도 작업을 이어갈 수 있어야 합니다.

- **현재 단계** — **2단계 완료(표 열넷 × 2 모두 ✓). 3단계 설계 대화 완료 + 파일 준비 끝, 타이핑 대기**
- **마지막으로 끝낸 것** — **3단계 설계 대화.** 활성화 함수를 **둘 다 `.identity`** 로 확정(STUDY Q7 — 사용자가 방향은 맞혔고, 어긋난 것은 «과장» 이 아니라 «구별이 사라진다» 였음). `BufferDescriptor` 다섯의 offset 을 **0·12·24·40·44 · stride 56** 으로 확정. **규칙 2 에 «묻기 전에 답할 수 있을 만큼 설명한다» 추가** — 모르는 개념을 그냥 물어 사용자가 답할 수 없었던 일이 계기. ~~2단계 뼈대 셋~~ `Shared/SplatBufferLayout.swift`(`enum` 이름 서랍 + `Splat` 확장으로 펴기/되돌리기) · `Steps/Step02_BufferRoundTrip.swift` · `Docs/Step02_BufferRoundTrip.md`(막힐 요소 다섯). **`LowLevelBuffer` API 를 문서에서 확인해 APPLE_DOCS 에 기록** — `Descriptor(capacity:sizeMultiple:)` · `init(descriptor:)` 가 **throws** · `withUnsafeBytes`/`withUnsafeMutableBytes` 의 **클로저 반환이 `Void`** · `bytesUsed` 는 `get set`
- **다음 할 일 — 사용자가 타이핑**
    1. 3단계 타이핑 — `SplatBufferLayout` 에 offset 다섯 추가 → `Step03_FirstSplat.swift` → `ContentView` 에 Section
    2. **Xcode 27 beta 로 열 것**(26.6 은 모름) · 눈 확인 : 뿌연 덩어리 하나 · 돌리면 납작해지는가
- **열린 것** — 스플랫 개수 상한의 정확한 값을 못 찾음. 문서는 «내부 제한» 이라고만 하고 `BufferResource` 초기화가 throw 로 알려 준다고 함. **7단계에서 실측해서 여기 적을 것**
- **막힌 것** — 없음

---

## 파일 지도

경로는 `gaussian-splat-ios-lab/` 기준입니다(`Docs/` 만 저장소 루트). 단계 파일은 `Steps/StepNN_이름.swift`, 설명서는 **같은 이름의** `Docs/StepNN_이름.md` 입니다.

| 경로 | 역할 | 상태 |
|---|---|:---:|
| `gaussian-splat-ios-lab/gaussian_splat_ios_labApp.swift` | 앱 진입점 | ✅ |
| `gaussian-splat-ios-lab/ContentView.swift` | 단계 목록 화면. 새 단계는 여기에 항목 추가 | ✅ |
| `Steps/Step00_Baseline.swift` | 0단계 · 흰 큐브 + GPU 패밀리 확인. **기준선** | ✅ |
| `Docs/Step00_Baseline.md` | 0단계 설명서. **「따라 칠 코드」 는 아직 비어 있음** (규칙 1 ⓓ) | ✅ |
| `Shared/Splat.swift` | 스플랫 하나를 담는 구조체. **3단계까지 씀** | ✅ |
| `Steps/Step01_SplatAsNumbers.swift` | 1단계 · 값 열넷 + 크기 표. **size 80 확인** | ✅ |
| `Docs/Step01_SplatAsNumbers.md` | 1단계 설명서. 「따라 칠 코드」 비어 있음 | ✅ |
| `Shared/SplatBufferLayout.swift` | `Splat` ↔ `[Float]` 열넷 변환. **8·9단계 파서까지 씀** | ✅ |
| `Steps/Step02_BufferRoundTrip.swift` | 2단계 · `LowLevelBuffer` **왕복 확인.** 그리지 않음 | ✅ |
| `Docs/Step02_BufferRoundTrip.md` | 2단계 설명서. **「따라 칠 코드」 채움** | ✅ |
| `Steps/Step03_FirstSplat.swift` | 3단계 · ★ **첫 타원체.** `BufferDescriptor` 다섯 | ✍️ |
| `Docs/Step03_FirstSplat.md` | 3단계 설명서. **따라 칠 코드 있음** | ✅ |
| `Resources/Splats/*.ply` | 스캔 파일. **커밋하지 않음** (`.gitignore`) | ⬜ |

상태 : ✅ 눈으로 확인 완료 · ✍️ 타이핑 대기 · 🟡 동작하나 남은 것 있음 · ⬜ 아직 없음
새 파일을 만들거나 지우면 **이 표를 같이 고칩니다.** 표가 낡으면 이 파일의 존재 이유가 사라집니다.

---

## 결정 기록

되돌리려는 유혹이 생길 만한 것만 남깁니다. 한 줄씩, 최대 10개. 넘치면 오래된 것부터 지웁니다.

- **visionOS 로 가지 않고 iOS 앱으로 만든다** — `GaussianSplatComponent` 가 iOS 27 에도 있음. 시뮬레이터를 안 쓰니 맥북 에어 부담이 사실상 0 이고 빌드-실행이 몇 초. visionOS 이관은 iOS 에서 확인이 끝난 뒤에
- **파일 파싱(8·9단계)을 앞에 두지 않는다** — PLY 를 먼저 건드리면 화면이 빌 때 «파서 / 버퍼 배치 / 렌더» 셋 중 어디가 틀렸는지 못 가름. 01~06 에서 **손으로 채운 숫자가 뜨는 것을 본 뒤에** 파일로 감
- **`try!` 를 쓰지 않는다** — 개수 상한은 `BufferResource` 초기화가 **throw** 로 알려 주는 유일한 통로. `try!` 는 그 정보를 크래시로 바꿈
- **`Splat` 구조체는 «학습용 발판» 이다 — 9단계쯤에서 버린다** — RealityKit 은 이 구조체를 본 적이 없다. 층이 셋(① PLY ② 구조체 ③ GPU 버퍼)인데 **②는 선택 사항**이고 실무는 ①→③ 직행이다(50만 개면 40MB 를 만들었다 버리는 셈). **그래도 지금 만드는 이유는 «내가 정한 배치 vs GPU 가 원하는 배치» 를 몸으로 알기 위해서.** 3단계까지는 개수가 적어 성능이 문제가 안 됨. 사용자가 «구조체 건너뛰고 바로 버퍼» 선택지를 보고도 A(지금대로)를 고름
- **`Splat` 의 필드 타입 — `SIMD3<Float>` 와 `simd_quatf`. 둘 다 «부딪히려고» 고른 것** — `SIMD3<Float>` 는 12가 아니라 **16바이트**(패딩), `simd_quatf` 는 메모리 순서가 **(ix,iy,iz,r)** 인데 Apple 버퍼는 **(r,x,y,z)** 를 요구. 그래서 구조체 전체가 **80바이트**(Apple 요구는 56). 크기는 2단계에서, 순서는 3단계에서 터지도록 **시간상 떨어뜨림**
- **규칙 1 에서 ⓒ(힌트 한 칸씩)를 없앤다 — 2026-09-03** — 「따라 칠 코드」를 ⓑ 와 **함께** 준다. 힌트 왕복이 토큰을 가장 많이 먹는 자리라 사용자가 정함. **ⓐ 설계 대화와 ⓑ 막힐 요소 예고는 그대로 두고**, 코드 뒤에 「왜 이렇게 썼나」 절을 붙여 막힐 요소에 답한다. 되돌리려면 CLAUDE.md 규칙 1 만 고치면 됨
- **3단계 활성화 함수는 둘 다 `.identity` — 8단계에서 뒤집힌다** — 손으로 채운 값은 이미 최종값이라 되돌릴 것이 없음. PLY 원값을 넘기는 8단계부터는 `.exponential`·`.sigmoid`. **«지금 맞는 답이 그때 틀린 답이 되는» 첫 자리**
- **버퍼를 «그리기 전에» 왕복으로 확인한다 (2단계)** — 바이트를 처음 만지는 자리와 3D 렌더링을 한꺼번에 하면 화면이 비었을 때 어느 쪽이 틀렸는지 못 가름. 「넣은 값 == 꺼낸 값」 을 먼저 통과시켜 두면 3단계에서 의심할 곳이 절반

---

## 열린 질문

답이 나오면 지우고, 개념 질문이면 STUDY.md·Q&A.md 로 옮깁니다.

- **스플랫 개수 상한이 정확히 몇인가** — 문서에 숫자가 없음. 7단계에서 개수를 늘려 가며 throw 가 나는 지점을 실측
- **Scaniverse 의 USDZ 내보내기를 Reality Composer Pro 3 이 바로 읽는가** — 읽는다면 8·9단계(파싱)를 통째로 건너뛸 수 있음. 다만 **건너뛰면 배울 것도 사라짐** — 9단계를 마친 뒤에 시험해 볼 것
- **왜 하필 Apple7 인가** — Apple 이 «필요하다» 고만 적고 이유를 안 밝힘. 스플랫 렌더가 «매 프레임 전체 정렬 + 타일 비닝» 이라는 컴퓨트 파이프라인이어서로 **추론**하지만 확인 못 함. Q&A 에 «확인된 것 / 추론» 을 갈라 적어 둠
- **인터리브(한 버퍼) vs 속성별 버퍼** — 13단계에서 opacity 만 매 프레임 갱신할 때 어느 쪽이 유리한가. 1단계에서 정하지만 **13단계에서 다시 볼 것**

---

## 세션 로그

최근 3개만 남깁니다. 넷째가 생기면 가장 오래된 것을 지웁니다.

- **2026-09-04 (9)** — **3단계 설계 대화 + 파일 준비.** Apple 문서에서 **`BufferDescriptor(buffer:format:stride:offset:)` · `BufferResource(...) throws` · `sphericalHarmonics` 는 튜플 · `format` 이 `MTLAttributeFormat`** 확인(APPLE_DOCS 에 일곱 줄). 다섯이 **한 버퍼를 offset 만 달리해 공유**. **STUDY Q7** — 활성화 함수. **규칙 2 개정** — 「묻기 전에 답할 수 있을 만큼 설명한다」
- **2026-09-04 (8)** — **2단계 타이핑 완료 + 코드 리뷰(규칙 12).** 어긋남 넷 — ① `infoRow("capacity", bytesUsed)` **이름 중복 버그** ② 두 파일에 `// 코드 작성:` 뼈대 잔해 ③ 결정이 끝났는데 질문형으로 남은 주석 ④ 오타. **주석을 흐름 번호 1️⃣~6️⃣ 로 다시 씀** — 파일 상단 지도와 함수 안 주석이 같은 번호로 이어지고, 번호마다 «입력·과정·출력». 사용자가 «버퍼를 옮기는 부분이 어딘지 모르겠다» 고 한 것이 계기. **규칙 9 를 «주석은 짧게» 로 개정**하고 규칙 8 의 지도 상한을 30줄 → 20줄로
- **2026-09-03 (6)** — **1단계 완료.** 실측 **size 80 · stride 80 · alignment 16** — 예측과 일치. **STUDY Q4·Q5·Q6** 기록(사용자가 Q4·Q5 를 스스로 맞힘). **수확 둘** — ① `~` 를 빼먹는 실수가 Q2 에 이어 두 번째라 **«비트로 하지 말고 말로 하세요»** 를 STUDY 에 못 박음 ② `stride` 는 «다음 것이 **정렬을 지키며** 시작할 수 있는 최소 간격» — 배열의 둘째 원소부터 어긋난다는 것을 사용자가 스스로 도달. **규칙 6 에 «단순 반복은 AI 가 채운다» 예외 추가**

---

## 어디에 무엇을 쓰나

같은 사실을 두 곳에 쓰지 않습니다. 중복은 낡은 쪽이 거짓말을 하게 만듭니다.

| 성격 | 파일 |
|---|---|
| 지금 어디까지 왔나, 다음에 뭘 하나 | **PROGRESS.md** (이 파일) |
| 단계별 계획과 설명, 완료 체크 | [README.md](README.md) 의 «진행 상황» 표 |
| 용어의 뜻 · 던진 문제와 그 수확 | [STUDY.md](STUDY.md) |
| 자주 여는 Apple 문서 | [APPLE_DOCS.md](APPLE_DOCS.md) |
| 막혔던 것과 그 **해결** | [Q&A.md](Q&A.md) |
| **아직 못 푼 것** — 재현되지만 우회 중 | [PROBLEMS.md](PROBLEMS.md) |
| AI 작업 규칙 — 목록 | [CLAUDE.md](CLAUDE.md) |
| 규칙의 취지와 지키는 방법 | [RULES.md](RULES.md) |
