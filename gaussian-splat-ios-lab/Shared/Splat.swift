//
//  Splat.swift
//  gaussian-splat-ios-lab / Shared
//
//  ─── 지도 ────────────────────────────────────────────────────────────
//
//  스플랫 «하나» 를 담는 값 타입. 1단계에서 만들어 3단계까지 쓴다.
//
//  Splat (struct)
//    ├ position : SIMD3<Float>   어디에 떠 있나          (x, y, z)
//    ├ scale    : SIMD3<Float>   세 축으로 얼마나 퍼졌나  (x, y, z)
//    ├ rotation : simd_quatf     그 퍼진 방향이 어느 쪽    (ix, iy, iz, r)
//    ├ opacity  : Float          얼마나 진한가            0…1
//    ├ sh       : SIMD3<Float>   무슨 색인가 (SH 0차)     r, g, b
//    └ sample                    화면에 찍어 볼 기본값 하나
//
//  ⚠️ 이 구조체는 «읽기 편하려고» 만든 것이지 «GPU 가 원하는 모양» 이 아니다.
//     Apple 이 요구하는 스플랫 하나는 56바이트인데 이것은 그보다 크다.
//     그 차이를 1단계에서 눈으로 확인하고, 2단계에서 해결한다.
//
//  📖 자세한 설명 → Docs/Step01_SplatAsNumbers.md
//  ─────────────────────────────────────────────────────────────────────

// 코드 작성: import 한 줄


/// 스플랫 하나가 갖는 값 열넷.
///
/// 「뿌연 타원체 하나」 를 만드는 데 필요한 전부다.
/// 중심에서 멀어질수록 옅어지는 3D 얼룩이고, 세 축의 퍼짐이 다르면 타원이 된다.
// 코드 작성: struct Splat


    /// 타원체의 중심이 어디에 있나. 단위는 미터.
    // 코드 작성: position


    /// 세 축 방향으로 얼마나 퍼져 있나.
    ///
    /// 셋이 같으면 흐릿한 공, 하나만 작으면 원반, 둘이 작으면 바늘.
    // 코드 작성: scale


    /// 그 퍼진 방향이 어느 쪽을 보나.
    ///
    /// ⚠️ `simd_quatf` 는 만들 때 `init(ix:iy:iz:r:)` 로 «실수부가 마지막» 이다.
    ///    Apple 의 스플랫 버퍼는 «r, x, y, z» 순서를 요구한다. 같지 않다.
    ///    1단계에서 실제 순서를 눈으로 확인한다.
    // 코드 작성: rotation


    /// 얼마나 진한가. 0 이면 안 보이고 1 이면 꽉 찬다.
    // 코드 작성: opacity


    /// 무슨 색인가. 구면조화 0차 계수 세 개 (r, g, b).
    ///
    /// 0차는 «어느 각도에서 봐도 같은 색» 이다. 각도에 따라 색이 변하게 하려면
    /// 1~3차 계수 45개가 더 필요한데, 이 저장소에서는 쓰지 않는다.
    // 코드 작성: sh


    /// 화면에 찍어 볼 기본값 하나.
    ///
    /// 값의 «그럴듯한 범위» 를 눈에 익히는 것이 목적이다.
    /// 이 범위가 9단계에서 PLY 파서를 디버깅할 때 잣대가 된다.
    // 코드 작성: sample


// 코드 작성: struct 를 닫는 중괄호
